# axon_quant 0.7.0 — L3 matching + per-fill observability + per-leg liquidity

> **Status**: Phase 0 (分支创建) + Phase 1 (per-fill observability) 完成
> **Base**: `origin/main` @ `9025062` (PR #32 = 0.6.0 merge)
> **Date**: 2026-07-18
> **Owner**: 0.7.0 work group

## Context — 0.6.0 残留 + 用户驱动的后续需求

**0.6.0 验证状况**:

- ✅ TestPyPI 0.6.0 wheel 已发布(2026-07-18 07:17),多 leg API 全部实现并可调用
- ✅ `set_target_position / push_funding / push_mark / with_auto_rebalance / with_auto_rebalance_disable / rebalance_to_target` 全部工作
- ✅ `RunResult.leg_targets / total_funding_pnl / rebalances_triggered / positions / marks` per-leg 字段就位
- ❌ 生产 PyPI 0.6.0 仍未发布(无 `v0.6.0` tag,无 `twine upload`)
- ⚠️ `RunResult.trades: list[dict]` 只记录 round-trip,不记录每笔 fill(用户认为"不完整",实际是设计如此,需要新增 L3 细节字段)

**用户驱动的 0.7.0 候选**(从对话上下文 + multi-leg backtest 实战中浮现):

| # | 需求 | 来源 | 状态 |
|---|------|------|------|
| F1 | per-leg seed liquidity(`with_seed_liquidity(instrument, half_spread, depth_levels, size_per_level)`) | 用户:`with_seed_liquidity` 不能 per-instrument | 0.6.0 局限,需 Rust API 扩展 |
| F2 | L3 matching engine(完整 order book L3 可见) | 0.6.0 plan 提过,延后到 0.7.0 | 新功能 |
| F3 | per-fill detail 字段(`RunResult.fills_detail: list[dict]`,每笔 fill 8 字段) | 用户:`trades` 缺开仓记录 | 新增 `fills_detail` 字段 |
| F4 | cross-leg risk(portfolio 级别 risk metrics) | 0.6.0 plan 已有 Phase,但 minimal | 深化 |
| F5 | 0.6.0 生产发布收尾 | 0.6.0 残留 | Phase 0 收口 |
| F6 | scheduler `*mut EventQueue` lifetime 重构(消除 `unsafe impl Send + Sync`) | unsafe 分析遗留 | 推到 0.7.0 或 0.8.0 |
| F7 | 0.7.0 wheel 重建 + TestPyPI 验证 | 发布闭环 | Phase 4 |

## Phases

### Phase 0:0.6.0 收口(在本分支第一 commit 前完成)

- [x] 创建 `0.7.0` 分支 from `origin/main` @ `9025062` (`8084a26`)
- [x] Push `origin/0.7.0`
- [ ] 打 `v0.6.0` tag + 推到生产 PyPI(等用户拍板)
- [ ] PR `0.6.0 → main`(已 merge,跳过)

### Phase 1:Per-fill observability(增量,不动 L3)

- [ ] **1.1** Rust 端:`BacktestEngineState.fills: Vec<FillRecord>`(已有 trades 之外的 fill 流)
- [ ] **1.2** `RunResult.fills_detail: Vec<FillRecord>` 新增 getter
- [ ] **1.3** PyO3 binding 暴露 `fills_detail: list[dict]`
- [ ] **1.4** Python 端 `backtest.py` docstring 注明 `trades`(round-trip)vs `fills_detail`(每笔)
- [ ] **1.5** 单测:`crates/axon-backtest/tests/test_fills_detail.rs`
  - 验证:同向加仓 → `fills=2, trades=0, fills_detail=2`
  - 验证:round-trip → `fills=2, trades=1, fills_detail=2`

### Phase 2:Per-leg seed liquidity

- [ ] **2.1** Rust 端:`SeedLiquidityConfig` 改为 `HashMap<Instrument, SeedLiquidityConfig>`(`HashMap<Instrument, SeedLiquidityConfig>`)
- [ ] **2.2** `BacktestEngine::with_seed_liquidity(instrument, half_spread, depth_levels, size_per_level)` 接受 instrument
- [ ] **2.3** 兼容路径:不传 instrument 时按 old API 给所有已 `set_target_position` 的 leg 各开一份
- [ ] **2.4** PyO3 签名:`with_seed_liquidity(self, half_spread, depth_levels, size_per_level, instrument=None)`(kwarg)
- [ ] **2.5** 单测 + integration test(spot+perp 不同 half_spread)

### Phase 3:L3 matching engine(可选,如时间允许)

- [ ] **3.1** 设计:`L3Book` = `BTreeMap<Price, Vec<L3Order>>`(`L3Order { order_id, side, qty, timestamp }`)
- [ ] **3.2** `axon-matching` 拆出独立 crate(可选,看代码复杂度)
- [ ] **3.3** `MatchingEngine` trait 化(L1 / L2 / L3 共用同一 `match(side, price, qty) -> Vec<MatchFill>` 接口)
- [ ] **3.4** `L3MatchingEngine` 实现:price-time priority,partial fills 记录到 `MatchFill.fills_detail`
- [ ] **3.5** 性能 gate:必须与 L2 同 case 跑 < 2x 慢(否则降级为可选 feature flag)

### Phase 4:Cross-leg risk 深化

- [ ] **4.1** `axon-risk` 新增 `PortfolioRiskEngine::delta_exposure()` per instrument
- [ ] **4.2** `PortfolioRiskEngine::gamma_exposure()`(per instrument + 跨 instrument 协方差)
- [ ] **4.3** `RunResult.risk_metrics: dict` 包含 per-leg delta / portfolio delta / vega / sharpe_with_legs
- [ ] **4.4** 单测 + e2e(spot+perp delta-neutral 实测 portfolio delta ≈ 0)

### Phase 5:发布

- [ ] **5.1** `make version-bump VERSION=0.7.0`
- [ ] **5.2** `make version-check`(三源对齐)
- [ ] **5.3** `make test-ci`(全过)
- [ ] **5.4** `mkdocs build --strict`(zh + en)
- [ ] **5.5** Maturin build wheel → TestPyPI
- [ ] **5.6** 装 TestPyPI 0.7.0 wheel,跑 4 个端到端测试(开仓/同向加仓/round-trip/perp funding+mark)
- [ ] **5.7** `git tag v0.7.0` + push
- [ ] **5.8** `twine upload` → 生产 PyPI(等用户拍板)

## Tasks vs Phases

| Phase | Tasks | 估算 | 关键风险 |
|-------|------:|-----:|---------|
| 0 | 3 | 0.5d | PyPI 凭证 |
| 1 | 5 | 1d | 极低 |
| 2 | 5 | 1.5d | API 兼容 |
| 3 | 5 | 3d | 性能 gate |
| 4 | 4 | 2d | 协方差数学 |
| 5 | 8 | 0.5d | wheel 平台兼容 |
| **Total** | **30** | **~8.5d** | |

## Risks / Open Questions

1. **L3 matching 是否要进 0.7.0?** — 性能 gate 可能拖累 release,如不达预期降为 `l3-matching` feature flag 推迟
2. **0.6.0 生产 PyPI 何时推?** — Phase 0 收口前确认用户拍板
3. **scheduler 重构(F6)** — blast radius 大,建议独立 0.8.0 PR
4. **`bytemuck` 替代 `mem::transmute`** — 收益小,先不做
5. **TestPyPI → 生产 PyPI 自动化** — `.github/workflows/release.yml` 已配但未跑过 v0.6.0 全流程,需 dry-run

## Next Steps

等用户拍板 Phase 1 开工(per-fill observability)还是先 Phase 0 收口(0.6.0 PyPI 发布)。
